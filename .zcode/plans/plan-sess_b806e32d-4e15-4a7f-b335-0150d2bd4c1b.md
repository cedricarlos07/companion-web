# COMPANION — Complete Frontend Build Plan

Build the full production-grade React frontend for Companion ("Votre entreprise n'oublie plus") inside `C:\Users\OSSEY\Pictures\COMPANION\HTML`, per the master prompt: React 19 + TypeScript + Vite + Tailwind v4 + BoardUI (free tier) + Hugeicons (free npm set), French UI, realistic francophone-African mock data, ~30 working routes.

## 0. Workspace preparation
- Archive the existing static export (`index.html`, `support.js`, `huge-icons.js`, `_ds/`, `avatars/`, `uploads/`, `.thumbnail`, `.DS_Store`) into `HTML/_legacy-export/` so nothing is destroyed, then scaffold fresh in `HTML/` root.
- Scaffold Vite 7 + React 19 + TS with npm; Tailwind v4; configure `@/` path alias (tsconfig + vite.config).

## 1. Foundations (Phase 1 of build order)
- **BoardUI**: `init_boardui` (free), then `install_components` for: button, icon-button, link-button, input, textarea, checkbox, checkbox-card, radio-card, switch, switch-card, select, dropdown, divider, file-upload, tabs, table, tooltip, badge, chip, status-dot, avatar, breadcrumb, pagination, segmented-control, notification, kbd, close-button, sidebar, settings-modal, auth-card, data-table, stat-cards, notification-center, agent-thinking, composer-loader.
- **Hugeicons**: `npm i @hugeicons/react @hugeicons/core-free-icons`. Central wrapper `components/ui/huge-icon.tsx` normalizing sizes (xs 14 / sm 16 / md 20 / lg 24 / xl 32), strokeWidth 1.5, currentColor, aria handling. Every icon import verified against the installed free package (compile fails otherwise — no invented names).
- **Theme**: BoardUI tokens, light default, primary indigo #625BF6, semantic green/amber/red/indigo/gray, Inter font, type scale per spec (page title 24–30px … caption 12px).
- **Types & data**: `src/types/` (Organization, Employee, Role, Memory, Source, Agent, Skill, Tool, Handover, Project, Client, Approval, Activity, Automation, Integration, Notification, Department) + `src/data/` typed domain files. Shared numbers used consistently everywhere: Kamaloka AI; 12,842 memories; 42 employees; 17 roles; 84% coverage; 6 critical risks; 4 active agents; 3 pending approvals; Moussa Koné 1,284 memories / 37 procédures / 18 uniques / 87% couverture; handover analysis 82% → pack 94%; Yann Kouassi onboarding 92% readiness / 38% progress; org risk 31%; the 8 named employees + clients Orange CI, SOTRA, NSIA, CIE, Ecobank (fictional demo context).

## 2. App shell & shared components
- `AppShell`: AppSidebar (248–264px, sections GENERAL/KNOWLEDGE/CONTINUITY/AGENTIC/CONTROL, workspace switcher Kamaloka AI, collapsible) + top context bar (search trigger, notification center, profile) + content area.
- `GlobalSearch` ⌘K: grouped results (People, Memories, Projects, Roles, Sources, Agents, Handovers…), Hugeicons per type, full keyboard navigation, navigates on Enter.
- Reusable per spec: PageHeader, StatCard, KnowledgeTypeBadge, KnowledgeStatusBadge, ConfidenceBadge, RiskBadge, AgentStatusBadge, AutonomyBadge, RoleBadge, SourceChip, SourceReference, PersonAvatar (initials+tint), MemoryCard/MemoryRow, CoverageProgress, HandoverReadiness, AgentThinkingPanel, FilterBar, EmptyState, ErrorState, skeleton LoadingState, DetailSidebar/drawer, ConfirmModal, Toasts.
- Free equivalents of Pro patterns (per your choice): ChatComposer (text/attach/context/agent/model/permission-mode row), AgentTaskList + AgentProgress (animated step checklist), InterviewQuestionnaire card.

## 3. All routes (each fully built, no placeholders)
`/login` · `/setup` (5-step wizard: Organisation, Intelligence, Sources, Équipe, Terminé) · `/home` (executive cockpit: stat cards, Santé de la mémoire, À traiter, agent activity, risk overview, recent knowledge) · `/ask` (context selector, prompt examples, composer, answer state with sources/confidence/actions) · `/brain` + `/brain/memory/:id` (evidence, history timeline, related knowledge, verify/edit/deprecate) · `/people` + `/people/:id` (unique-knowledge section, tabs) · `/roles` + `/roles/:id` (Role Brain, contributor timeline showing survival across turnover) · `/sources` + `/sources/new` (upload/connect/paste, processing pipeline, extraction results) · `/knowledge-risk` (31% overall, ranked role bars, critical dependencies) · `/handovers` + `/handovers/new/:employeeId` (agent-thinking live steps) + `/handovers/:id` (readiness, coverage, gaps, 9-tab pack, actions) + `/handovers/:id/interview` (6/9 questions, NEW PROCEDURE result) · `/onboarding` + `/onboarding/:id` (Yann journey sections) · `/agents` + `/agents/new` (6-step wizard) + `/agents/:id` (goal, memory access, tools, action permissions, pause/run) · `/automations` (WHEN/IF/THEN/DO cards + builder) · `/approvals` (tabs, action + memory-update cards, approve/reject updates state) · `/activity` (filterable audit timeline) · `/integrations` (categories + full MCP server/client section) · `/settings` (left nav: General, Organization, Members, AI Providers, Memory, Agents, Security, Permissions, Storage, Backup, Notifications, Appearance, Advanced).

## 4. Interactivity (no dead UI)
Working router nav, tabs, table filters/sort/search/pagination, ⌘K palette, wizard step state with form validation, approvals change status + toast, agent pause/resume/run mutates mock state, notifications dismissible, interview advances and produces the new procedure card, source ingestion animates its pipeline, toasts on all primary actions.

## 5. Verification
- `tsc` + `vite build` clean; grep audit: zero lucide/heroicons/material/fontawesome imports, zero fake Hugeicons names.
- Run dev server; browser-automation visual pass across every route (screenshots), checking layout coherence, French microcopy, consistency of shared numbers, empty/loading/error states, responsive at 1440/1280/1024; fix all findings before delivery.
